{
  "thresholds": {
    "max_file_length": 400,
    "pre_flight_trigger_lines": 150,
    "human_checkpoint_files": 2
  },
  "safety": {
    "exempt_from_blast_radius": [
      "tests/",
      "docs/",
      "scripts/"
    ]
  },
  "lint": {
    "exclude_paths": []
  },
  "secrets": {
    "allowlist": []
  },
  "reference_selection": {
    "rules": [
      {
        "patterns": ["README.md", "CHANGELOG.md", "docs/", "references/"],
        "references": ["references/operations.md"]
      },
      {
        "patterns": ["tools/", "bin/", "install.js", "package.json"],
        "references": ["references/safety.md"]
      },
      {
        "patterns": ["templates/", ".github/", "release"],
        "references": ["references/operations.md", "references/research-and-fit.md"]
      },
      {
        "patterns": ["mcp", "MCP"],
        "references": ["references/mcp-governance.md"]
      }
    ]
  },
  "external_evidence": {
    "providers": {
      "github_code_scanning": {
        "category": "code_scanning",
        "required": false,
        "artifact": "SARIF upload / Code Scanning alerts",
        "trust_limit": "Source-located findings only; not session governance proof."
      },
      "github_artifact_attestations": {
        "category": "build_provenance",
        "required": false,
        "artifact": "GitHub artifact attestation",
        "trust_limit": "Proves build artifact provenance, not source security."
      },
      "npm_trusted_publishing": {
        "category": "package_provenance",
        "required": false,
        "artifact": "npm provenance attestation",
        "trust_limit": "Proves package publish source, not governance compliance."
      },
      "agent_security": {
        "category": "agent_security",
        "required": false,
        "artifact": "Provider-specific MCP or agent security output",
        "trust_limit": "Requires approved MCP/tool credentials before trusted use."
      }
    }
  },
  "risk_policy": {
    "actions": {
      "read_only": {
        "tier": "low",
        "description": "Inspect files, run local read-only commands, or gather context."
      },
      "file_edit": {
        "tier": "medium",
        "description": "Create or modify declared in-scope files."
      },
      "dependency_change": {
        "tier": "high",
        "description": "Add, remove, upgrade, or replace project dependencies."
      },
      "external_network": {
        "tier": "high",
        "description": "Transmit data to external services or fetch version-specific evidence."
      },
      "release_publish": {
        "tier": "high",
        "description": "Publish packages, tags, releases, or deployment artifacts."
      },
      "destructive_command": {
        "tier": "blocked",
        "description": "Delete, reset, overwrite, or force-push repository state."
      },
      "secret_exposure": {
        "tier": "blocked",
        "description": "Print, store, transmit, or commit credentials or private tokens."
      }
    }
  }
}
